<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="server.persistence.WikiMapper">

    <select id="select" resultType="server.business.Wiki">
        SELECT
            id_article,
            nom_article
        FROM
            astanble.article
    </select>

</mapper>
